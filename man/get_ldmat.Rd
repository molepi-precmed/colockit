% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ldmat.R
\name{get_ldmat}
\alias{get_ldmat}
\title{Compute an LD correlation matrix from a PLINK reference panel}
\usage{
get_ldmat(region, refplinkfile)
}
\arguments{
\item{region}{A \code{data.table} with columns \code{snp} (rsid, e.g. \code{"rs123"}),
\code{dbsnpid} (integer), and \code{allele} (effect allele, one of A/C/G/T).}

\item{refplinkfile}{Full path to the PLINK reference panel (without
extension; \code{.bed}, \code{.bim}, \code{.fam} files must exist at that path).}
}
\value{
A named list with two entries:
\itemize{
\item \code{region} — the input \code{data.table} filtered to SNPs that could be
matched in the reference panel and are not monomorphic.
\item \code{ldmat} — a symmetric numeric matrix of LD correlations (r, not r²),
with row and column names set to rsids.
Returns \code{NULL} if no SNPs from \code{region} are found in the reference.
}
}
\description{
Reads a PLINK binary reference panel using \code{bigsnpr}, filters to SNPs
present in \code{region}, removes monomorphic variants, and returns a
signed LD correlation matrix with allele orientation matched to the
effect alleles in \code{region}.
}
\details{
This function is shared by both the standalone and genoscores backends.
}
\keyword{internal}
