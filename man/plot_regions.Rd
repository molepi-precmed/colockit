% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_regions}
\alias{plot_regions}
\title{Generate locus plots and colocalisation visualisations}
\usage{
plot_regions(
  src_region,
  trgt_region,
  locus,
  plot_file,
  ldmat = NULL,
  coloc = NULL
)
}
\arguments{
\item{src_region}{A \code{data.table} of GWAS 1 summary statistics. Required
columns: \code{snp}, \code{dbsnpid}, \code{chrom}, \code{pos}, \code{pvalue}, \code{beta},
\code{trait.name}.}

\item{trgt_region}{A \code{data.table} of GWAS 2 summary statistics (same
requirements as \code{src_region}).}

\item{locus}{A single-row \code{data.table} with columns \code{chrom}, \code{startpos},
\code{endpos}.}

\item{plot_file}{Full path to the output PNG file.}

\item{ldmat}{Optional LD correlation matrix (required for colocalisation
mode). Default \code{NULL}.}

\item{coloc}{Optional \code{data.table} or \code{data.frame} with columns \code{snp} and
\code{SNP.PP.H4} (per-SNP posterior probability of H4). Default \code{NULL}.}
}
\value{
A named list with entries \code{src} and \code{trgt} containing only the
SNPs common to both GWAS within the locus.
Returns \code{NULL} if there are no common SNPs.
}
\description{
Dual-mode plotting function:
\itemize{
\item \strong{Preview mode} (default, \code{coloc = NULL}): plots both GWAS across the
locus, highlighting SNPs common to both in red.
\item \strong{Colocalisation mode} (\code{coloc} and \code{ldmat} supplied): colours SNPs by
their posterior probability of H4, labels the top SNP, and appends LD
heatmaps beneath each locus plot.
}
}
\keyword{internal}
