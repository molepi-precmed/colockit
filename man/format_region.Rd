% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{format_region}
\alias{format_region}
\title{Format a harmonised GWAS region for the chosen colocalisation method}
\usage{
format_region(region, analysis_type, ldmat)
}
\arguments{
\item{region}{A harmonised GWAS \code{data.table}. Required columns:
\code{snp}, \code{pos}, \code{beta}, \code{varbeta}, \code{pvalue}, \code{freq}, \code{n_samples}.
Additional columns \code{allele}, \code{oth.allele}, \code{se} are needed for
\code{"pwcoco"}.}

\item{analysis_type}{One of \code{"abf"}, \code{"susie"}, or \code{"pwcoco"}.}

\item{ldmat}{An LD correlation matrix whose row/column names are rsids
matching \code{region$snp}. May be \code{NULL} for \code{"abf"} (LD is optional for
ABF).}
}
\value{
For \code{"abf"} / \code{"susie"}: a named list suitable for
\code{\link[coloc:coloc.abf]{coloc::coloc.abf()}} or \code{\link[coloc:coloc.susie]{coloc::coloc.susie()}}.
For \code{"pwcoco"}: a \code{data.table} with columns \code{snp}, \code{eff.allele},
\code{oth.allele}, \code{freq}, \code{beta}, \code{se}, \code{pvalue}, \code{n_samples}.
}
\description{
Reshapes a harmonised GWAS \code{data.table} into the exact input structure
expected by each method.
}
\details{
For \code{"abf"} and \code{"susie"} the output is a named list validated by
\code{\link[coloc:check_dataset]{coloc::check_dataset()}}. For \code{"pwcoco"} the output is a flat
\code{data.table}.
}
\keyword{internal}
