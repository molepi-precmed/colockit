% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_regions.R
\name{prepare_regions}
\alias{prepare_regions}
\title{Prepare GWAS regions for colocalisation analysis}
\usage{
prepare_regions(
  locus,
  gwas1,
  gwas2,
  output_dir,
  refplinkfile,
  backend = c("standalone", "genoscores"),
  gwas_type = c("published", "mixed", "private"),
  con = NULL
)
}
\arguments{
\item{locus}{A single-row \code{data.table} with integer columns \code{chrom},
\code{startpos}, and \code{endpos} defining the genomic region of interest.}

\item{gwas1}{For \code{backend = "standalone"}: a \code{data.table} of summary
statistics for trait 1 (see Details).
For \code{backend = "genoscores"}: an integer gwasid, or a \code{data.table} of
private summary statistics when \code{gwas_type = "private"}.}

\item{gwas2}{Same as \code{gwas1} but for trait 2.}

\item{output_dir}{Full path to the directory where intermediate files will
be saved (\code{gwas1.csv}, \code{gwas2.csv}, \code{ld.matrix.Rdata.gz},
\code{locus.plot.png}).}

\item{refplinkfile}{Full path to the PLINK reference panel used to compute
the LD matrix (without file extension; \code{.bed}, \code{.bim}, \code{.fam} must
exist).}

\item{backend}{Which data backend to use. One of \code{"standalone"} (default)
or \code{"genoscores"}.}

\item{gwas_type}{\emph{(genoscores backend only)} One of \code{"published"},
\code{"mixed"}, or \code{"private"}.}

\item{con}{\emph{(genoscores backend only)} A DBI database connection to the
Genoscores database.}
}
\value{
A named list with entries:
\itemize{
\item \code{src} — harmonised \code{data.table} for trait 1
\item \code{trgt} — harmonised \code{data.table} for trait 2
\item \code{ldmat} — LD correlation matrix (rsid-named)
}

Returns \code{NULL} if no common SNPs remain after all filtering steps.
}
\description{
Stage 1 of the colockit pipeline. Harmonises two GWAS datasets within a
specified genomic locus, validates allele frequencies, computes an LD
matrix, and writes intermediate files to \code{output_dir}.
}
\details{
Dispatches to one of two backends depending on the \code{backend} argument:
\itemize{
\item \code{"standalone"} — no database dependency; all inputs supplied directly.
\item \code{"genoscores"} — integrates with the Genoscores database to retrieve
published GWAS or generate private GWAS via SNPTEST.
}

\strong{Standalone backend — required columns for \code{gwas1} / \code{gwas2}:}
\describe{
\item{\code{snp}}{rsid string, e.g. \code{"rs123456"}}
\item{\code{dbsnpid}}{integer dbSNP identifier}
\item{\code{chrom}}{integer chromosome number}
\item{\code{pos}}{integer genomic position (hg38)}
\item{\code{allele}}{effect allele (A/C/G/T)}
\item{\code{oth.allele}}{other allele}
\item{\code{beta}}{effect size}
\item{\code{pvalue}}{association p-value}
\item{\code{n_samples}}{sample size}
\item{\code{freq}}{effect allele frequency}
\item{\code{trait.name}}{character label for the trait}
}
}
\examples{
\dontrun{
## Standalone backend ------------------------------------------------
locus <- data.table::data.table(
  chrom    = 22L,
  startpos = 29001000L,
  endpos   = 29011000L
)

## gwas1 and gwas2 must be data.tables with columns:
## snp, dbsnpid, chrom, pos, allele, oth.allele,
## beta, pvalue, n_samples, freq, trait.name
regions <- prepare_regions(
  locus        = locus,
  gwas1        = gwas1,
  gwas2        = gwas2,
  output_dir   = "path/to/output",
  refplinkfile = "path/to/kg.2020.hg38.eur",
  backend      = "standalone"
)

## Genoscores backend ------------------------------------------------
con <- DBI::dbConnect(...)
regions <- prepare_regions(
  locus        = locus,
  gwas1        = 42L,   # integer gwasid
  gwas2        = 57L,
  output_dir   = "path/to/output",
  refplinkfile = "path/to/kg.2020.hg38.eur",
  backend      = "genoscores",
  gwas_type    = "published",
  con          = con
)
}

}
\seealso{
\code{\link[=colocalise]{colocalise()}} for Stage 2.
}
