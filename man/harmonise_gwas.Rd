% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonise.R
\name{harmonise_gwas}
\alias{harmonise_gwas}
\title{Harmonise effect alleles between two GWAS regions}
\usage{
harmonise_gwas(src, trgt, locus, output_dir, find_alleles_fn = NULL)
}
\arguments{
\item{src}{A \code{data.table} of GWAS summary statistics for trait 1.}

\item{trgt}{A \code{data.table} of GWAS summary statistics for trait 2.}

\item{locus}{A single-row \code{data.table} with columns \code{chrom}, \code{startpos},
\code{endpos} defining the region.}

\item{output_dir}{Full path to the output directory.}

\item{find_alleles_fn}{A function used to resolve missing \code{oth.allele}
values. Defaults to \code{NULL} (missing alleles cause those SNPs to be
dropped). The genoscores backend supplies its own DB-backed resolver.
The function must accept a \code{data.table} of rows with missing alleles
and return a \code{data.table} with columns \code{dbsnpid}, \code{allele},
\code{oth.allele}.}
}
\value{
A named list with entries \code{src} and \code{trgt}, each a harmonised
\code{data.table} containing only SNPs common to both GWAS.
}
\description{
Ensures that effect alleles are aligned between \code{src} and \code{trgt}.
Where the effect allele differs, \code{beta} and the allele labels in \code{src}
are flipped to match \code{trgt}. SNPs with irreconcilable allele mismatches
are dropped from both datasets.
}
\details{
Calls \code{\link[=plot_regions]{plot_regions()}} as a side-effect to produce a preliminary locus
plot highlighting common SNPs.
}
\keyword{internal}
