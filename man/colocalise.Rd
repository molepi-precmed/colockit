% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colocalise.R
\name{colocalise}
\alias{colocalise}
\title{Run colocalisation analysis on prepared GWAS regions}
\usage{
colocalise(
  regions,
  locus,
  analysis_type = c("abf", "susie", "pwcoco"),
  output_dir,
  refplinkfile = NULL,
  cutoff = 0.05
)
}
\arguments{
\item{regions}{A named list with entries \code{src}, \code{trgt} (harmonised GWAS
\code{data.table}s) and \code{ldmat} (LD correlation matrix), as returned by
\code{\link[=prepare_regions]{prepare_regions()}}.}

\item{locus}{A single-row \code{data.table} with columns \code{chrom}, \code{startpos},
\code{endpos}.}

\item{analysis_type}{Colocalisation method. One of:
\itemize{
\item \code{"abf"} — coloc Approximate Bayes Factor
\item \code{"susie"} — coloc SuSiE (handles multiple causal variants)
\item \code{"pwcoco"} — PWCoCo (external command-line tool)
}}

\item{output_dir}{Full path to the directory where results will be saved.}

\item{refplinkfile}{Full path to the PLINK reference panel. Required for
\code{"pwcoco"}. Default \code{NULL}.}

\item{cutoff}{P-value cut-off for the stepwise SNP selection in PWCoCo.
Default \code{0.05}.}
}
\value{
For \code{"abf"} / \code{"susie"}: the coloc result object (list with
\verb{$summary} and \verb{$results}). For \code{"pwcoco"}: a \code{data.table} of PWCoCo
output.  Returns \code{NULL} if SuSiE fails.
}
\description{
Takes the output of \code{\link[=prepare_regions]{prepare_regions()}} and runs the chosen
colocalisation method. Saves formatted input data and a colocalisation
plot to \code{output_dir}.
}
\examples{
\dontrun{
## After running prepare_regions():
result <- colocalise(
  regions       = regions,
  locus         = locus,
  analysis_type = "abf",
  output_dir    = "path/to/output"
)

## Inspect posterior probabilities:
result$summary

## SuSiE (handles multiple causal variants):
result_susie <- colocalise(
  regions       = regions,
  locus         = locus,
  analysis_type = "susie",
  output_dir    = "path/to/output"
)
}

}
\references{
\strong{PWCoCo:} Robinson JW, Hemani G, Babaei MS, Huang Y, Baird DA, Tsai EA,
Chen C-Y, Gaunt TR, Zheng J (2022). An efficient and robust tool for
colocalisation: Pair-wise Conditional and Colocalisation (PWCoCo).
\emph{bioRxiv}. \doi{10.1101/2022.08.08.503158}
}
