% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-standalone.R
\name{prepare_regions_standalone}
\alias{prepare_regions_standalone}
\title{Prepare GWAS regions — standalone backend}
\usage{
prepare_regions_standalone(locus, gwas1, gwas2, output_dir, refplinkfile)
}
\arguments{
\item{locus}{A single-row \code{data.table} with columns \code{chrom} (integer),
\code{startpos} (integer), \code{endpos} (integer).}

\item{gwas1}{A \code{data.table} of summary statistics for trait 1. Required
columns: \code{snp} (rsid), \code{dbsnpid} (integer), \code{chrom}, \code{pos}, \code{allele}
(effect allele), \code{oth.allele}, \code{beta}, \code{pvalue}, \code{n_samples}, \code{freq}
(effect allele frequency), \code{trait.name}.}

\item{gwas2}{A \code{data.table} of summary statistics for trait 2 (same
column requirements as \code{gwas1}).}

\item{output_dir}{Full path to the output directory.}

\item{refplinkfile}{Full path to the PLINK reference panel (without
extension).}
}
\value{
A named list with entries \code{src}, \code{trgt} (harmonised \code{data.table}s)
and \code{ldmat} (LD correlation matrix), or \code{NULL} if no common SNPs remain.
}
\description{
Accepts user-supplied summary statistics directly with no database
dependency. LD is computed from a PLINK reference panel via \code{\link[=get_ldmat]{get_ldmat()}}.
}
\keyword{internal}
